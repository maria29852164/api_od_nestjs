export enum typesDatabase {
  increment = 'increment',
  varchar = 'varchar',
  integer = 'integer',
  nullable = 'nullable',
  unique = 'unique',
  email = 'email',
  text = 'text',
  boolean = 'boolean',
  timestamp = 'timestamp',
  created_at='created_at'
}
